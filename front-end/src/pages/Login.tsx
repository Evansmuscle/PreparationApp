import { AuthLayout } from "@layout";
import React from "react";

interface Props {
  //
}

const Login: React.FC<Props> = ({}) => {
  return (
    <AuthLayout>
      <div>Login Page</div>
    </AuthLayout>
  );
};

export default Login;
